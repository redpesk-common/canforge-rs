# deny.toml

# (optional) keep the graph small by ignoring dev-dependencies
[graph]
exclude-dev = true

[licenses]
# Everything is denied by default—list what your org allows here.
allow = [
  "MIT",
  "Apache-2.0",
  "Zlib",
  "Unicode-3.0",   # required by unicode-ident
  "BSL-1.0",       # Boost
  "BSD-3-Clause",
]
